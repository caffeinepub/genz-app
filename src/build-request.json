{
  "kind": "build_request",
  "title": "Enable PWA “Install to Home Screen” for Genz App",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-49",
      "text": "Add PWA installability metadata by creating a web app manifest and linking it from the app HTML so mobile browsers can offer “Install to Home Screen” for Genz App.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-install"
        ],
        "quotes": [
          "install to home screen"
        ]
      },
      "acceptanceCriteria": [
        "A `manifest.webmanifest` file exists under the frontend public/static serving path and contains: app name “Genz App”, a short_name, start_url, display mode suitable for standalone install, theme_color/background_color values, and icon entries for at least 192x192 and 512x512 images.",
        "`frontend/index.html` includes a `<link rel=\"manifest\" ...>` referencing the manifest.",
        "User-facing name shown by the install prompt is “Genz App”."
      ]
    },
    {
      "id": "REQ-50",
      "text": "Add a service worker that enables basic PWA behavior for installability and provides safe, minimal offline support for the static app shell without interfering with authenticated API calls.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-install"
        ],
        "quotes": [
          "install to home screen"
        ]
      },
      "acceptanceCriteria": [
        "A service worker file exists under the frontend public/static serving path and is registered by code that runs at runtime (without editing immutable `frontend/src/main.tsx`).",
        "The service worker precaches core static assets needed to load the UI shell (e.g., index.html and built JS/CSS assets) and uses a conservative strategy that does not cache authenticated backend requests.",
        "With the device in airplane mode after a successful first load, reopening the installed app shows at least a basic offline page/shell (not a browser network error)."
      ]
    },
    {
      "id": "REQ-51",
      "text": "Implement an in-app install prompt UI that appears only when installation is available, and provide clear manual “Add to Home Screen” instructions for iOS Safari where the standard install prompt is not available.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-install"
        ],
        "quotes": [
          "install to home screen"
        ]
      },
      "acceptanceCriteria": [
        "On supported browsers (e.g., Chrome/Edge Android), when the `beforeinstallprompt` event is available, the app displays an “Install Genz App” call-to-action (e.g., in the header or landing page) that triggers the native install prompt when clicked.",
        "On iOS Safari, the app displays a user-facing help/instructions UI (English) explaining how to add to home screen (e.g., “Tap Share, then ‘Add to Home Screen’.”).",
        "The install UI does not show once the app is already installed (detect installed display-mode) and does not show if the browser does not support installation.",
        "All new user-facing text is in English."
      ]
    },
    {
      "id": "REQ-52",
      "text": "Update `frontend/index.html` with appropriate PWA-related meta tags for better mobile install experience.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-install"
        ],
        "quotes": [
          "install to home screen"
        ]
      },
      "acceptanceCriteria": [
        "`frontend/index.html` includes `theme-color` meta consistent with the manifest.",
        "`frontend/index.html` includes iOS-related meta/link tags as appropriate (e.g., `apple-touch-icon` and/or `apple-mobile-web-app-capable`) pointing to existing public icon assets.",
        "The document title is updated to a user-appropriate title (e.g., “Genz App”) rather than scaffold text."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under `frontend/src/hooks/useInternetIdentity.ts`, `frontend/src/hooks/useInternetIdentity.tsx`, `frontend/src/hooks/useActor.ts`, `frontend/src/main.tsx`, or anything under `frontend/src/components/ui` (compose these components instead).",
    "Do not introduce additional backend canisters/services; keep backend unchanged unless strictly required for installability (it should not be).",
    "All user-facing text added or modified must be in English."
  ],
  "nonGoals": [
    "Generating an Android APK file.",
    "Implementing push notifications.",
    "Implementing background sync or complex offline-first data caching for dynamic provider/job data.",
    "Adding third-party authentication beyond Internet Identity."
  ],
  "imageRequirements": {
    "required": [
      "App icon (pwa-icon.dim_192x192.png)",
      "App icon (pwa-icon.dim_512x512.png)",
      "Maskable app icon (pwa-icon-maskable.dim_512x512.png)"
    ],
    "edits": []
  }
}